<h1>Your Trips</h1>

<%= link_to "New Trip", new_trip_path %>

<ul>
  <% @trips.each do |trip| %>
    <li>
      <%= link_to trip.name, trip_path(trip) %> 
      (<%= trip.start_date %> - <%= trip.end_date %>)
    </li>
  <% end %>
</ul>
